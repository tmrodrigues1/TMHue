<Window x:Class="TMHue.App.Views.UpdateToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextFormattingMode="Display">
    <!-- Margem externa dá espaço para a sombra, no mesmo padrão do CaptureToastWindow. -->
    <Grid Margin="24">
        <Border Background="#F514131A"
                BorderBrush="#2EFFFFFF"
                BorderThickness="1"
                CornerRadius="12"
                Padding="16,12">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.45" BlurRadius="20" ShadowDepth="4" Direction="270" />
            </Border.Effect>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="24" Height="24" CornerRadius="8" Margin="0,0,12,0" VerticalAlignment="Center"
                        BorderBrush="#40FFFFFF" BorderThickness="1">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#5B8DF6" Offset="0" />
                            <GradientStop Color="#8B5CF6" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="&#xE896;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                               Foreground="#FFFFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <TextBlock x:Name="MessageText" FontFamily="Segoe UI"
                           FontSize="13" Foreground="#FFF5F4F8" VerticalAlignment="Center" Margin="0,0,12,0" />
                <Button x:Name="UpdateButton" Content="{DynamicResource L.Settings.Update}" Click="OnUpdateClick"
                        FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold"
                        Foreground="#FFFFFFFF" Background="#5B8DF6" BorderThickness="0"
                        Padding="14,5" Cursor="Hand" VerticalAlignment="Center">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="#6E9BF8" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bg" Property="Background" Value="#3A3947" />
                                    <Setter Property="Foreground" Value="#FF8A8894" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button x:Name="DismissButton" Content="&#xE711;" Click="OnDismissClick"
                        FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="#FF8A8894"
                        Background="Transparent" BorderThickness="0" Padding="8,6" Margin="4,0,0,0"
                        Cursor="Hand" VerticalAlignment="Center">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
